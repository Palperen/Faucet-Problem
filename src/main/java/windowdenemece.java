import javax.swing.*;
import java.awt.*;

public class windowdenemece extends JPanel{
    public void paintComponent(Graphics g){
        super.paintComponent(g);
        g.setColor(Color.lightGray);
        g.fillRect(0, 0, getWidth(), getHeight());

    }







}
